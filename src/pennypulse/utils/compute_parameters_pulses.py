def compute_amplitude_rotation(angle: float, duration: float):
    """Computes the amplitude for so the rotation with a constant pulse has
    this angles for the given duration"""
    return angle / duration
